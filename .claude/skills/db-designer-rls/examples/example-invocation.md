Invoke the skill:

/db-designer docs/requirements/payments.md target=branch

Or freeform:

/db-designer "Add multi-tenant projects, tasks, and comments with RLS. Users belong to orgs. Need audit log." target=dev
