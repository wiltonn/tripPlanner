# ──────────────────────────────────────────────────────────────
# TripPlanner — Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to .env.local (repo root) and fill in the values.
# For Vercel deployment, add these in the Vercel dashboard under
# Settings → Environment Variables.
#
# After deploying to Vercel, add your production URL to Supabase:
#   Dashboard → Authentication → URL Configuration → Redirect URLs
#   Add: https://your-app.vercel.app/**
# ──────────────────────────────────────────────────────────────

# --- Public (exposed to browser) ---
NEXT_PUBLIC_MAPBOX_TOKEN=          # Mapbox public access token (pk.*)
NEXT_PUBLIC_SUPABASE_URL=          # Supabase project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=     # Supabase anon/public key

# --- Server-only (never exposed to browser) ---
MAPBOX_SECRET_TOKEN=               # Mapbox secret token for Directions API proxy (sk.*)
SUPABASE_SERVICE_ROLE_KEY=         # Supabase service role key (if used server-side)

# --- Local dev only (not needed on Vercel) ---
API_PORT=4000                      # Fastify API port (local dev)
DATABASE_URL=                      # Direct Postgres connection (local dev)
