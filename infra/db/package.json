{
  "name": "@trip-planner/db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0"
  },
  "devDependencies": {
    "prisma": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
